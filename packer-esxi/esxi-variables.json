{
    "esxi_host": "{{ env `ESXI_HOST`}}",
    "esxi_datastore": "{{ env `ESXI_DS`}}",
    "esxi_username": "{{ env `HV_USER`}}",
    "esxi_password": "{{ env `HV_PASS`}}",
    "vmname": "{{ env `VMNAME`}}"
}
